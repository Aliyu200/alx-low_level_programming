#include "main.h"

/**
 * _isalpha - check for alphabetic character
 * @c: The character to be checked
 * Return: 1 for alphaetic character or 0 for anything else
 */

int _isalpha(int c)

{
	if ((C >= 65 && c <= 90) || (C >= 97 && c <= 122))
	{
	return (1);
	}
	return (0);
}
